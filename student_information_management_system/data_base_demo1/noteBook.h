#ifndef NOTEBOOK_H
#define NOTEBOOK_H
/*
 *1、首先先创建一个新的设计师界面类文件，不妨取名为sqldelete完成数据库的删除操作 -> 自动生成对应的CPP与.h和ui文件
 *2、在.h中声明变量、槽函数(public slots:)、信号等
 *3、在ui界面设计框架，并转到CPP文件中进行实现
 *4、在主窗口中增加信号函数，利用ui类父类指针(QWidget等)指向一个新生成的对象，并将该对象显示出来
 *5、在该对象对应的类的cpp文件中结合ui设计对功能进行实现
 *
 *
*/
#endif // NOTEBOOK_H
